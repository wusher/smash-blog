!!!
%head
  - title = yield(:page_title)

  %title= "#{title.present? ? title + " ~ " : ""   }witty title"
  = stylesheet_link_tag    "application", :media => "all" 
  = javascript_include_tag "application" 
  = csrf_meta_tags 

%body
    
  .liner
    .line_one
      .dark
    .line_two
      .dark
    .line_three
      .dark
    .line_four
      .dark
  %header.nav
    %h1 
      = link_to "wusher", root_path
    = render partial: "layouts/navigation"
  .container.body
    .notice= notice
    .alert= alert
    = yield
  %footer.footer
    .liner
      .line_four
        .dark
      .line_three
        .dark
      .line_two
        .dark
      .line_one
        .dark
