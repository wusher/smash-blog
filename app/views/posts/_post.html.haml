%article.post.row
  %header.three.columns.info.alpha
    %h2.title
      =link_to post.title, post_path(post.slug)
    %timedate.pubdate{ pubdate: "pubdate"}= post.pubdate.present? && post.pubdate.strftime("%m/%d/%y")
  .ten.columns.post-body!= post.body_html
  .three.columns.omega
    
