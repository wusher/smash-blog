%article.post.sixteen.columns
  .row
    %header.two.columns.info.alpha
      .title
        %a{ :href => "posts/#{post.slug}" }
          %strong= post.title
      %timedate.pubdate{ pubdate: "pubdate"}= post.pubdate.strftime("%m/%d/%y")
    .fourteen.columns.omega.body!= post.body_html
